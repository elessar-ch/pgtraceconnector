receivers:
  otlp:
    protocols:
      grpc:
        endpoint: localhost:14317
      http:
        endpoint: localhost:14318

exporters:
  debug:
    verbosity: detailed

processors:
  batch:

connectors:
  example:
    attribute_name: db.name

service:
  pipelines:
    traces:
      receivers: [example]
      exporters: [debug]
    logs:
      receivers: [otlp]
      exporters: [example]